<!DOCTYPE html>
<html>
<head>
  <title>Midterm</title>
  <link href="//netdna.bootstrapcdn.com/font-awesome/4.0.3/css/font-awesome.css" rel="stylesheet">
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
  <div class="container-fluid">
    <nav class="navbar navbar-default navbar-fixed-top" role="navigation">
      <div class="container">
        <%= link_to "Home", root_path , class: "btn btn-default" %>

        <% if user_signed_in? %>
          <span class="pull-right" style="padding:10px">Hello <%= current_user.full_name %>&nbsp;</span>
          <%= link_to "Sign Out", destroy_user_session_path, method: :delete, class: "btn btn-default pull-right" %>
        <% else %>
          <%= link_to "Sign In", new_user_session_path, class: "btn btn-default pull-right" %>
        <% end %>

        <%= link_to "New Idea", new_idea_path, class: "btn btn-default pull-right" %>
        <%= link_to "Ideas", ideas_path, class: "btn btn-default pull-right" %>
      </div>
    </nav>

<div class="space"></div>

      <% if flash[:notice] %>
        <div class="alert alert-success">
          <%= flash[:notice] %>
        </div>
      <% elsif flash[:alert] %>
        <div class="alert alert-danger">
          <%= flash[:alert] %>
        </div>
      <% end %>


      <%= yield %>


  </div>

</body>
</html>
